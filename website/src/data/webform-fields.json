{
  "_comment": "LEADCF -> CRM field map, established EMPIRICALLY on 2026-08-15 by submitting one probe lead per index and reading the result back via COQL. Zoho exposes no API for this mapping and the embed page does not hydrate on direct navigation, so probing is the only reliable source. Re-verify after any webform edit — indices are assigned by Zoho and can change.",
  "_verified": "2026-08-15",
  "_method": "one probe POST per LEADCF index; COQL read-back; probe records deleted",
  "fields": {
    "LEADCF1": "Lead_Type",
    "LEADCF3": "Lead_Source_Detail",
    "LEADCF9": "WhatsApp_Number",
    "LEADCF10": "UTM_Source",
    "LEADCF11": "UTM_Medium",
    "LEADCF12": "UTM_Campaign"
  },
  "native_fields": ["Last Name", "Company", "Email", "Phone", "Description"],
  "_unmapped": {
    "Consent_Given": "boolean — not reachable via LEADCF2,4,5,6,7,8 with value 'true'. Index unknown.",
    "Consent_Timestamp": "datetime — index unknown",
    "Consent_Policy_Version": "text — index unknown",
    "_note": "All three are ON the webform canvas (verified visually in the builder) but their LEADCF indices were not found by probing 2-12. Zoho may name checkbox/datetime inputs differently. Resolve by reading the generated embed HTML."
  }
}
